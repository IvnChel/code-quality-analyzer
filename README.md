
# 🚀 Python Code Quality Analyzer

[![CI/CD Pipeline](https://github.com/IvnChel/code-quality-analyzer/actions/workflows/main.yml/badge.svg)](https://github.com/ТВОЙ_ЛОГИН/ТВОЙ_РЕПО/actions)

## 🎯 Обзор проекта
Этот проект — автоматизированный инструмент для анализа качества кода на Python. Он помогает студентам и разработчикам соблюдать стандарты PEP 8, отслеживать сложность функций и находить дубликаты. 

**Почему это полезно?** В контексте образования (Data Science/ML) это позволяет быстро проверять студенческие работы на чистоту кода и давать мгновенную обратную связь через отчеты.

## ✨ Ключевые функции
- 📊 **Метрики:** подсчет строк, количества функций и классов.
- 🧠 **Сложность:** расчет цикломатической сложности (кол-во ветвлений `if`, `for`, `while`).
- 🐍 **Стиль:** проверка имен функций на соответствие `snake_case`.
- 📝 **Документация:** проверка наличия Docstrings в функциях.
- ✂️ **Анти-копипаст:** обнаружение дублирующихся строк кода.

## 🛠 Установка
1. Клонируйте репозиторий:
   ```bash
   git clone [https://github.com/ТВОЙ_ЛОГИН/ТВОЙ_РЕПО.git](https://github.com/ТВОЙ_ЛОГИН/ТВОЙ_РЕПО.git)
   cd code-quality-analyzer

```

2. Установите зависимости:
```bash
pip install -r requirements.txt

```



## 🚀 Использование

Запустите основной скрипт для анализа кода в папке `data`:

```bash
python src/main.py

```

После завершения будет создан файл `full_report.txt` с подробным разбором.

## 📈 Пример работы

Допустим, в папке `data` лежит файл `bad_code.py`:

```python
def BadFunction(x):
    if x > 0:
        if x < 10:
            print(x)
    print(x)

```

**Результат анализа в отчете:**

* ❌ **Стиль:** Функция `BadFunction` должна быть в `snake_case`.
* ❌ **Документация:** Отсутствует Docstring.
* ⚠️ **Сложность:** Высокая вложенность условий.

## 🤖 CI/CD и Креатив

В проекте настроен продвинутый GitHub Actions Workflow:

1. **Авто-тесты:** Запуск `pytest` при каждом пуше.
2. **Артефакты:** После каждой проверки CI/CD генерирует отчет и сохраняет его в **GitHub Artifacts**. Вы можете скачать результат анализа прямо из вкладки Actions!
3. **Ручной запуск:** Кнопка `workflow_dispatch` позволяет запустить анализ в любой момент без изменения кода.

## 📂 Структура проекта

* `src/` — ядро анализатора.
* `tests/` — модульные тесты.
* `data/` — примеры кода для проверки.
* `docs/` — подробная логика метрик.
---





